# Server
PORT=5000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:80

# Database
MONGO_URI=mongodb://127.0.0.1:27017/hrms

# Auth
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=1d
COOKIE_NAME=hrms_token

# Email / SMTP — leave SMTP_USER & SMTP_PASS blank to use the console stub.
# For Gmail: SMTP_PASS must be a 16-char App Password (Google Account >
# Security > 2-Step Verification > App passwords), NOT your login password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=vikram.vicky1241@gmail.com
SMTP_PASS=jeintfqaqqaznqvs
MAIL_FROM=XYZ HRMS <vikram.vicky1241@gmail.com>

# Initial admin seed (npm run db:seed)
SEED_ADMIN_EMAIL=admin@xyz.com
SEED_ADMIN_PASSWORD=ChangeMe!123
SEED_ADMIN_FIRST_NAME=Admin
SEED_ADMIN_LAST_NAME=System
